#:schema node_modules/wrangler/config-schema.json

name = "roast-prof"
main = "src/worker/index.ts"
compatibility_date = "2026-03-10"
compatibility_flags = ["nodejs_compat"]

upload_source_maps = true

[placement]
mode = "smart"

[assets]
directory = "./dist"
binding = "ASSETS"

[observability]
enabled = true
head_sampling_rate = 1

# Runtime vars — set real values via `wrangler secret put` or CF dashboard for production.
# For local dev, override in .dev.vars (git-ignored).
[vars]
BASE_URL = "https://api.openai.com/v1"
MODEL = ""
SITE_URL = "https://roast-prof.workers.dev"
SITE_NAME = "Roast Prof"
SITE_CATEGORIES = "entertainment,education"
# API_KEY must be set as a secret: `wrangler secret put API_KEY`
